{
  "name": "blog",
  "version": "1.0.0",
  "scripts": {
    "start": "npm run build && browser-sync start --server --watch --no-open",
    "build": "node make.js"
  },
  "dependencies": {
    "browser-sync": "^2.26.12",
    "html-minifier": "^4.0.0"
  },
  "devDependencies": {
    "jsdom": "^16.4.0"
  }
}
